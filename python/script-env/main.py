import argparse

def main():
    parser = argparse.ArgumentParser(
        prog='GitLab API Script',
        description='A simple script for retrieving information from GitLab via GitLab API')
    
    ## Define scripts arguments
    parser.add_argument 

if __name__==main:
    main()